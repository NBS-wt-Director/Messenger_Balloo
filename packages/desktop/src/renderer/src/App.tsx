// App.tsx — Desktop renderer root component
// Wraps the web app with Electron-specific providers and desktop layout

import DesktopApp from './DesktopApp';

function App() {
  return <DesktopApp />;
}

export default App;