{
  "name": "@balloo/ui",
  "version": "1.0.0",
  "private": true,
  "description": "Balloo Messenger — единый UI-контракт: Topbar/Footer/ThemeProvider/I18nProvider/токены/темы (тик. №1 мультитикета поддоменов)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --noEmit --watch",
    "test": "echo \"No tests configured yet\"",
    "lint": "echo \"No linter configured yet\""
  },
  "dependencies": {
    "@balloo/shared": "workspace:*",
    "zustand": "^4.5.0"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.5.0"
  }
}
