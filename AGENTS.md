# AGENTS.md — Balloo Messenger Monorepo

> Этот файл — постоянная инструкция для AI-ассистента (Koda).
> Действует во всех новых сессиях. Перечитывай при каждом старте работы с проектом.

---

## 🔐 Системные данные

- **sudo пароль:** `131013`
- **PostgreSQL:** `postgresql://balloo:balloo123@localhost:5432/balloo?schema=public`
- **Пользователь PostgreSQL:** `balloo` / `balloo123`
- **БД PostgreSQL:** `balloo`

---

## 📌 Главный принцип

**Макеты (`mockups/`) — единственный источник правды.**

Макеты определяют:
1. Структуру экранов приложения
2. Дизайн (визуальное оформление, компоненты, цветовая система)
3. Функционал (что экран делает, какие взаимодействия поддерживает)

Документация (`docs/`, `index_ecrans.md`, `index_ecrans.json`, MD-файлы рядом с макетами) и код (`packages/`) — **производные от макетов**. Сначала макет → потом документация → потом код.

---

## 🔒 КРИТИЧЕСКОЕ ПРАВИЛО: Границы воркспейса

**Воркспейс проекта:** `/home/ivan/Рабочий стол/проекты/balloo`

1. **ВСЕ операции** (чтение, запись, создание, удаление, перемещение) выполняются **СТРОГО ВНУТРИ** воркспейса.
2. **НИКОГДА** не создавать, не редактировать и не удалять файлы вне воркспейса без **явной письменной команды** пользователя.
3. **Всегда проверять падеж** в путях: `проекты/` (именительный), а НЕ `проектов/` (родительный). Ошибка в падеже создаёт дубликат проекта и ломает структуру.
4. **Без тикета и triple-подтверждения** не выходить за пределы воркспейса. Не использовать `/home/ivan/Рабочий стол/`, `/tmp/`, домашнюю директорию, другие проекты.
5. При работе с внешними файлами (например, `general_files/`) — использовать только абсолютные пути, указанные в тикете, и копировать, а не перемещать.
6. **Нарушение этого правила = критическая ошибка.** При сомнениях — спросить, а не предполагать.
7. **ПЕРЕД УДАЛЕНИЕМ** любого файла или папки — выполнить `ls -laR` и показать содержимое пользователю, получить явное разрешение.
8. **НИКОГДА не удалять** папки, созданные с ошибкой в пути, без проверки: там может быть код, не закоммиченный в git.

---

## 🔁 Порядок проектирования (универсальный)

**Этот порядок применяется во ВСЕХ проектах на этой машине.**

При добавлении любой новой функциональности в проект, проектирование идёт строго в следующем порядке:

1. **Функция** (`0_xx_yy`) — что делает новый функционал, какая бизнес-задача решается
2. **Узел** (`у_xx`) — какому узлу (домену) принадлежит функция; если функция затрагивает несколько узлов — описывается для каждого
3. **Пользовательская ситуация** — сценарий использования: кто, что делает, в какой последовательности, какой результат
4. **Экраны** (`1_xx_yy`) — какие UI-экраны нужны для этой функции (по ситуациям)
5. **Эндпоинты + Компоненты** (`2_xx_yy` + `4_xx_yy`) — API-эндпоинты и UI-компоненты, которые обслуживают экраны
6. **Сущности данных** (`3_xx_yy`) — таблицы/модели БД, которые нужны для хранения данных функции

**Никогда не начинать проектирование с сущностей данных или эндпоинтов.** Сначала понимаем «что и зачем» (функция + ситуация), потом «как выглядит» (экраны), потом «как устроено под капотом» (API + данные).

---

## 🔢 Система нумерации объектов

Все объекты проекта имеют уникальные ID в формате **`z_xx_yy`**, где:

| Префикс `z` | Тип объекта | Пример |
|---|---|---|
| `у` | Узел (node) | `у_01` — узел balloo.su |
| `0` | Функция (function) | `0_01_03` — функция №3 узла №1 |
| `1` | Экран (screen) | `1_01_04` — экран №4 узла №1 |
| `2` | API-эндпоинт | `2_01_05` — эндпоинт №5 узла №1 |
| `3` | Сущность данных (data entity) | `3_01_02` — сущность №2 узла №1 |
| `4` | Компонент (component) | `4_01_01` — компонент №1 узла №1 |

- **`xx`** — номер узла (01–99), к которому принадлежит объект
- **`yy`** — порядковый номер элемента внутри узла (01–99)
- **Узел `00`** — зарезервирован для **общих экранов** (`shared/`), не принадлежащих конкретному домену
- Нумерация **внутри узла независима** — добавление экрана в один узел не сдвигает нумерацию других
- ID присваиваются при создании объекта и **не меняются** при правках
- При удалении объекта его ID **не переиспользуется** (остаётся свободным)

### Номера узлов (фиксированные)

| № | ID узла | Узел |
|---|---|---|
| 01 | `у_01` | balloo.su — основной мессенджер |
| 02 | `у_02` | admin.balloo.su — админ-панель |
| 03 | `у_03` | command.balloo.su — портал сотрудников |
| 04 | `у_04` | features.balloo.su — фич-реквесты |
| 05 | `у_05` | history.balloo.su — история версий |
| 06 | `у_06` | download.balloo.su — загрузки |
| 07 | `у_07` | docs.balloo.su — API документация |
| 08 | `у_08` | mobile — сборные мобильные макеты |
| 09 | `у_09` | desktop — обёртки окна ПК |
| 10 | `у_10` | specifity.balloo.su — спецификация (при разработке: слева описание, справа экран) |
| 11 | `у_11` | blog.balloo.su — корпоративный блог |
| 00 | `у_00` | shared — общие экраны (ошибки, офлайн, бан, обжалование) |

---

## 📦 Общие экраны (узел `shared/`)

Экраны, которые повторяются в нескольких узлах с минимальными отличиями (ошибки, офлайн, maintenance), создаются **один раз** в каталоге `mockups/shared/` и имеют ID с префиксом `xx = 00`.

### Принципы

1. **Один макет — много узлов.** Общий экран создаётся в `shared/`, а узлы на него ссылаются (в коде — через shared-компонент)
2. **Не плодить дубликаты.** Если экран в двух узлах отличается только текстом/лого — это общий экран с параметризацией, не два отдельных макета
3. **В индексах** общие экраны перечислены только в узле `shared` с полем `used_by` — списком узлов, которые их используют
4. **В узлах-потребителях** общие экраны НЕ дублируются как отдельные записи — только ссылка в метаданных

### Состав общих экранов

| ID | Экран | Используется в узлах |
|---|---|---|
| `1_00_01` | Ошибка 404 (не найдено) | все, кроме docs |
| `1_00_02` | Ошибка 403 (доступ запрещён) | все, кроме docs |
| `1_00_03` | Ошибка 500 (внутренняя ошибка) | все, кроме docs |
| `1_00_04` | Ошибка 503 (сервис недоступен) | все, кроме docs |
| `1_00_05` | Офлайн (авторизованный) | все, кроме docs |
| `1_00_06` | Офлайн (гость / неавторизованный) | все, кроме docs |
| `1_00_07` | Ошибка 555 (аккаунт заблокирован / бан) | все, кроме docs |
| `1_00_08` | Обжалование бана (оверлей) | все, кроме docs |

### Когда создавать общий экран

- Экран **существует в 2+ узлах** и отличается только брендингом/текстом → общий
- Экран уникален для одного узла → обычный экран в каталоге узла
- При сомнении — создавать отдельный экран в узле (безопаснее)

---

## 🔄 Жизненный цикл проекта

### Фаза 1 — Проектирование макетов (ТЕКУЩАЯ ФАЗА)

- Пользователь даёт замечания/правки по макетам → я вношу изменения
- Пользователь просит добавить экран → я создаю макет
- Пользователь просит добавить функцию в узел → я:
  1. Добавляю/обновляю экраны в макетах, если для этой функции нужен UI
  2. Обновляю `index.html`, `index_ecrans.md`, `index_ecrans.json`
  3. Создаю/обновляю MD-документацию рядом с макетом
- Пользователь просматривает экраны в `index.html`, отмечает узлы просмотренными
- **Готовность = `??`** пока пользователь не просмотрит все узлы и не подтвердит

### Фаза 2 — Фиксация документации (по команде пользователя)

Команда: **«зафиксируй документацию и начинай разработку»**

- Все макеты замораживаются — больше не меняются без явного перехода к v2
- Я генерирую/обновляю полную документацию в `docs/` на основе финальных макетов и документации из `docs/`
- `index_ecrans.json` — статусы узлов переводятся в `задокументирован` → `спроектирован`
- **Я создаю/обновляю `README.md`** в корне проекта — единый входной документ для разработчиков:
  - Описание проекта и его назначение
  - Стек технологий
  - Структура монорепо (краткая схема)
  - Инструкция по запуску (установка зависимостей, dev-сервер, сборка)
  - Ссылки на ключевую документацию (`docs/`, `mockups/`)
  - Ссылки на макеты экранов (`mockups/index.html`)
  - Скрипты монорепо (pnpm workspaces)
  - Статус проекта (готовность, версия)
  - Лицензия
- Начинается разработка в `packages/`
- по поводу компонентов работаю с каталогом компонентов `/home/ivan/Рабочий стол/проекты/general_files/components/`
- данные о компании, логотипы, маскота беру из `/home/ivan/Рабочий стол/проекты/general_files/company` (маскот  и логотип текущего проекта либо создаю сам либо в поддиректории {название_проекта} из `/home/ivan/Рабочий стол/проекты/general_files/company`)
### Фаза 3 — Разработка

- Макеты не меняются (только чтение как эталон)
- Код в `packages/` должен 100% совпадать с макетами по дизайну
- Статусы в `index_ecrans.json` обновляются: `написан` → `протестирован` → `готов`
- **Статус:** ✅ Завершена 2026-07-30. Все 66 тикетов выполнены, релиз v1.0.0 готов.

### Фаза 4 — v2 (по команде пользователя)

- Макеты снова можно менять/добавить
- Новый цикл правок → фиксация → разработка v2
- Отложенные задачи: см. `tickets/deferred-to-v2.md` (14 задач)
- Приоритеты v2 будут определены после сбора фидбека от пользователей

---

## ⚙️ Порядок обработки замечаний по макетам

Когда пользователь даёт замечание/просьбу по макету, я выполняю шаги **строго в этом порядке**:

### Шаг 1 — Внести правку в макет

- Открыть нужный HTML-файл макета (`mockups/<узел>/<экран>.html`)
- Внести запрошенные изменения
- Если пользователь просит новый экран — создать HTML-файл
- Если пользователь просит новую функцию в узел — проверить, нужен ли для неё экран; если да — создать/обновить
- Соблюдать дизайн-систему из `mockups/assets/common.css` и `mockups/assets/common.js`
- Не нужно спрашивать у пользователя, какие файлы менять — я сам знаю структуру

### Шаг 2 — Создать/обновить MD-документацию рядом с макетом

- Файл: `mockups/<узел>/<экран>.md` (рядом с HTML)
- Содержимое — **задание на создание работоспособной страницы приложения**:
  - Описание страницы
  - Структура (topbar, sidebar, content, секции)
  - Дизайн-требования (дизайн-система, компоненты, темы)
  - Компоненты (React-компоненты, которые нужно создать)
  - API (endpoints, WebSocket events)
  - Адаптивность / мобильные взаимодействия
  - Технологии
- Если MD уже существует — обновить
- Если макет новый — создать MD

### Шаг 3 — Обновить `index.html`

- `mockups/index.html` — главная страница-каталог
- Добавить карточку нового экрана в нужный узел
- Обновить количество экранов в статистике
- Обновить chip счётчика в заголовке узла
- Если добавлен новый узел — создать секцию с метаданными
- Обновить баннер готовности (количество узлов/экранов)

### Шаг 4 — Обновить `index_ecrans.md`

- `mockups/index_ecrans.md` — Markdown-документация метаданных
- Обновить сводную таблицу узлов (количество экранов, компоненты, функции)
- Добавить/обновить запись экрана в таблице узла
- Обновить путь к MD-документации в колонке «Документация»
- Если добавлена новая функция — обновить список функций узла

### Шаг 5 — Обновить `index_ecrans.json`

- `mockups/index_ecrans.json` — структурированные метаданные (JSON)
- Обновить `total_screens`, `screens_unviewed`
- Добавить/обновить объект экрана в массиве `screens` узла
- Обновить `app_functions` и `app_functions_count` если функция новая
- Обновить `components_private_planned` / `components_global_planned` если компоненты изменились
- Указать путь к MD в поле `doc_md` экрана
- Обновить `last_updated` (дата)
- Если правка по существующему экрану — увеличить `edits_specific` у узла
- Если правка глобальная (затронула несколько узлов) — увеличить `edits_global_accepted` у затронутых узлов

### Шаг 6 — Обновить глобальную документацию (`docs/`)

- **Этот шаг выполняется всегда при добавлении новой функции** — не только при фиксации, но и в текущей фазе проектирования
- Если добавлена новая функция в узел — обновить релевантные файлы в `docs/`:
  - `01-architecture-decisions.md` — если функция влияет на архитектуру
  - `02-requirements-checklist.md` — добавить/обновить требование
  - `03-database-schema.md` — если нужны новые таблицы/поля
  - `04-api-websocket-spec.md` — если нужны новые endpoints/events
  - `05-frontend-spec.md` — если меняется фронтенд-спецификация
  - `06-devops-infrastructure.md` — если меняется инфраструктура
- Если функция не требует изменений в глобальной документации — шаг пропускается
- Глобальная документация в `docs/` живёт параллельно макетам: макет описывает **как выглядит**, документация описывает **как устроено под капотом**
- `docs/` будет финализирован в Фазе 2, но правки вносятся уже сейчас — чтобы не копить технический долг

---

## 📁 Структура проекта

```
balloo/
├── AGENTS.md                    ← этот файл (постоянная инструкция)
├── docs/                        ← документация проекта (генерируется из макетов)
│   ├── 00-master-build-guide.md
│   ├── 01-architecture-decisions.md
│   ├── 02-requirements-checklist.md
│   ├── 03-database-schema.md
│   ├── 04-api-websocket-spec.md
│   ├── 05-frontend-spec.md
│   └── 06-devops-infrastructure.md
├── mockups/                     ← ИСТОЧНИК ПРАВДЫ (макеты экранов)
│   ├── index.html               ← каталог всех макетов
│   ├── index_ecrans.json        ← метаданные (JSON)
│   ├── index_ecrans.md          ← метаданные (Markdown)
│   ├── assets/
│   │   ├── common.css           ← общая дизайн-система
│   │   └── common.js            ← общая интерактивность
│   ├── shared/                  ← общие экраны (ошибки, офлайн) — узел 00
│   ├── balloo-su/               ← 24 экрана (основной мессенджер) — узел 01
│   ├── admin-balloo-su/         ← 15 экранов (админ-панель) — узел 02
│   ├── command-balloo-su/       ← 16 экранов (портал сотрудников) — узел 03
│   ├── features-balloo-su/      ← 5 экранов + MD (фич-реквесты) — узел 04
│   ├── history-balloo-su/       ← 4 экрана + MD (changelog) — узел 05
│   ├── download-balloo-su/      ← 2 экрана + MD (загрузки) — узел 06
│   ├── docs-balloo-su/          ← 1 экран + MD (API docs) — узел 07
│   ├── mobile/                  ← 7 экранов + MD (сборные мобильные) — узел 08
│   ├── desktop/                 ← 5 экранов + MD (обёртки окна ПК) — узел 09
│   ├── specifity-balloo-su/     ← 1 экран + MD (спецификация) — узел 10
│   └── blog-balloo-su/          ← 6 экранов + MD (корпоративный блог) — узел 11
├── packages/                    ← код монорепо (пока пусто)
│   ├── web/src/
│   ├── desktop/src/
│   ├── mobile-android/
│   ├── mobile-ios/
│   ├── server/src/
│   └── shared/src/
└── docker/                      ← Docker-инфраструктура
```

---

## 🎨 Дизайн-система (краткая справка)

Подробно — в `mockups/assets/common.css`. Ключевые принципы:

- **Аватарки** — восьмигранные (октагон, `clip-path: var(--octagon-clip)`) с двойной рамкой (контекст + статус)
- **Пузыри сообщений** — без скруглений, с угловыми срезами (`--bubble-sender`, `--bubble-receiver`)
- **Темы** — 3 шт.: dark (по умолчанию), light, russian (флаг РФ + драгметаллы)
- **Glassmorphism** — `backdrop-filter: blur()`, полупрозрачные карточки
- **Без border-radius** — везде прямые углы, кроме switch и аватарок
- **Шрифт** — Inter / Manrope
- **Акцент** — `--accent: #2db84d` (зелёный)
- **Языки** — 20 (3 группы): Русские (RU + 14 языков народов РФ: TT, BA, CE, CV, AV, DAR, UDM, LEZ, KBD, CHM, OS, SAH, BUA, UK), Дружественные (ZH, HI, BE), Остальные (EN, FR). Эталон: `mockups/data_schema.json` → `conventions.languages`. Перечень подтверждён пользователем.

---

## 🚦 Статусы реализации

| Статус | Описание |
|---|---|
| `запланирован` | Узел/экран описан в планах |
| `задокументирован` | Написана документация |
| `спроектирован` | Макет готов, архитектура определена |
| `написан` | Код написан, не протестирован |
| `протестирован` | Код протестирован, готов к релизу |
| `готов` | Релизнут. Дополнительно: `открыт` / `закрыт` |

---

## ⚡ Быстрые правила

1. **Макет → MD → index.html → index_ecrans.md → index_ecrans.json → docs/** — всегда в этом порядке
2. **Не спрашивать** у пользователя, какие файлы менять — я знаю структуру
3. **Новая функция → проверить нужен ли экран** → если да, создать/обновить → **затем обновить `docs/`**
4. **Готовность `??`** — пока пользователь не подтвердит, что просмотрел все узлы
5. **Фаза фиксации** — только по явной команде пользователя
6. **После фиксации** — макеты только для чтения (до v2)
7. **common.css / common.js** — общие для всех макетов, менять осторожно (затронет все экраны)
8. **MD рядом с макетом** — это задание разработчику, не просто описание
9. **JSON — машиночитаемый**, MD — человекочитаемый, index.html — интерактивный каталог
10. **`docs/`** — глобальная документация под капотом, обновляется параллельно макетам при добавлении функций
11. **README.md** — создаётся в Фазе 2 (фиксация), единый входной документ для разработчиков
12. **Нумерация `z_xx_yy`** — уникальные ID для всех объектов; узел `00` = общие экраны (`shared/`)
13. **Общие экраны** — создаются один раз в `shared/`, узлы ссылаются на них; не дублировать в узлах-потребителях
14. **Сущности данных** — вся информация о таблицах/моделях БД, полях, типах, ссылках и предзаполнении лежит в `mockups/data_schema.json` (схема) и `mockups/pre_filled_data.json` (seed). Подробности — в разделе «🗄️ Сущности данных» ниже
15. **🔒 ВОРКСПЕЙС — СВЯТОЕ:** Пути `проекты/` (не `проектов/`), работаем ТОЛЬКО внутри `/home/ivan/Рабочий стол/проекты/balloo`. Без явной команды — НИЧЕГО не трогаем вне воркспейса.

---

## 🎫 Мультитикеты (глобальное правило для всех монорепо)

**Мультитикет** — это документ для решения большой задачи, который разбивает задачу на несколько сессий.

### Как создаётся
1. Пользователь просит решить большую задачу (просмотр 134 экранов, рефакторинг всего проекта, генерация всей документации и т.д.)
2. Я создаю мультитикет-документ: разбиваю задачу на логические подзадачи (тикеты №1, №2, №3...)
3. Каждый тикет описан с чёткими критериями завершения
4. **Мультитикет сохраняется в каталог `tickets/` текущего монорепо** (НЕ на рабочий стол)
5. **Сразу после создания мультитикта — показываю инструкцию по его полному завершению**

### Как выполняется
- Команда запуска: `прочитай документ: [путь_к_мультитикету] и выполни тикет № X`
- Каждый подтикет выполняется **строго в рамках одной сессии**
- После выполнения тикета — пользователь подтверждает результат или указывает правки
- Следующий тикет запускается новой командой

### После создания мультитикта всегда показывать:
1. Ссылку на созданный документ (путь внутри монорепо: `tickets/имя.md`)
2. Количество тикетов и их содержание
3. **Инструкцию по полному завершению:**
   - Выполняйте команды sequentially: `прочитай документ: [путь] и выполни тикет № 1`, затем `... № 2` и т.д.
   - После каждого тикета — подтвердите результат или укажите правки
   - После последнего тикета — задача считается завершённой
   - Готовность проекта пересматривается

---

## 🗄️ Сущности данных (источник правды по данным)

**Вся информация о сущностях данных проекта хранится в двух файлах в `mockups/`:**

| Файл | Назначение | Содержимое |
|---|---|---|
| `mockups/data_schema.json` | **Схема данных** | 81 таблица / 24 группы: поля, типы данных (String, BigInt, Boolean, Int, Json, Float), nullable/unique/default, описания, ссылки (foreign keys → table.field + relation), fill_type (seed/runtime), конвенции (PK cuid, timestamps BIGINT, стек PostgreSQL 16 / Prisma / Flyway / Supabase) |
| `mockups/pre_filled_data.json` | **Предзаполненные данные (Seed)** | Эталонные seed-данные для 10 таблиц с `fill_type=seed`: `donate_tiers`, `onboarding_steps` + переводы, `text_pages` + переводы, `blog_categories` + переводы, `knowledge_categories`, `application_stages`, `feature_categories`. Переводы на 6 языков (ru, en, zh, fr, be, hi) |

### Правила работы с сущностями данных

1. **Единственный источник правды по данным** — `mockups/data_schema.json` и `mockups/pre_filled_data.json`. Секция «Данные» в `mockups/index.html` — визуальное представление этих файлов (производное).
2. **При добавлении/изменении сущности данных** (таблицы, поля, типа, связи) — сначала правка в `mockups/data_schema.json`, затем синхронизация секции «Данные» в `mockups/index.html`, затем `docs/03-database-schema.md` (Prisma-схема).
3. **При добавлении/изменении seed-данных** — сначала правка в `mockups/pre_filled_data.json`, затем при необходимости обновить `docs/03-database-schema.md` (раздел seed).
4. **Порядок:** `data_schema.json` / `pre_filled_data.json` → `index.html` (секция Данные) → `docs/03-database-schema.md` → код (`packages/server`).
5. **Конвенции** (из `data_schema.json`): PK — `String @id @default(cuid())`; `createdAt`/`updatedAt` — `BigInt` (Unix timestamp в секундах); FK — поле `<refTable_singular>Id`; enum-значения хранятся как `String`.
6. **`docs/03-database-schema.md`** — детальная Prisma-спецификация (машиночитаемые модели). JSON-файлы — структурированный реестр сущностей для генерации и валидации. Оба источника должны быть синхронны.
7. **Runtime-таблицы** (заполняются в работе приложения) — только в `data_schema.json`, в `pre_filled_data.json` их нет.
8. **Seed-таблицы** (`fill_type: seed`) — одновременно в `data_schema.json` (описание полей) и в `pre_filled_data.json` (эталонные строки).

---

## 🎨 Фиксация темы оформления

**Темы оформления (3 шт.: dark, light, russian) — ЗАФИКСИРОВАНЫ.**

- Эталон: `mockups/index.html` (переключатель тем в топбаре)
- Все три темы (dark, light, russian) определены однозначно — их CSS-переменные в `mockups/assets/common.css` являются истиной
- **Любые замечания пользователя по теме «тем оформления», «цветовых схем», «визуального стиля тем» — ИГНОРИРОВАТЬ** до перехода в Фазу 2 (v2)
- Это указание действует во всех сессиях монорепо и не подлежит обсуждению

---

## 🐛 Хаки и обходные пути (Workarounds)

### Проблема: `edit_file` не работает с кириллическими путями

**Симптом:** `edit_file` возвращает "File not found" для файлов в директориях с русскими символами (например, `Рабочий стол/проекты/...`).

**Решение:** Использовать метод "создал новый → удалил старый → переименовал":

1. Создать новый файл с `_` в начале названия (например, `_profile.html`)
2. Вписать полное содержимое с изменениями
3. Убедиться, что новый файл создан успешно
4. Удалить старый файл (`rm`)
5. Переименовать новый (`mv _file.html file.html`)

**Почему работает:** `create_new_file` и `run_terminal_command` корректно работают с кириллическими путями через `bash`, а `edit_file` имеет баг с кодировкой путей.

**Глобальное правило:** Всегда использовать этот хак для файлов в директориях с русскими символами в имени.


---

## 🐛 Хаки и обходные пути (Workarounds)

### Проблема: `edit_file` не работает с кириллическими путями

**Симптом:** `edit_file` возвращает "File not found" для файлов в директориях с русскими символами (например, `Рабочий стол/проекты/...`).

**Решение:** Использовать метод "создал новый → удалил старый → переименовал":

1. Создать новый файл с `_` в начале названия (например, `_profile.html`)
2. Вписать полное содержимое с изменениями
3. Убедиться, что новый файл создан успешно
4. Удалить старый файл (`rm`)
5. Переименовать новый (`mv _file.html file.html`)

**Почему работает:** `create_new_file` и `run_terminal_command` корректно работают с кириллическими путями через `bash`, а `edit_file` имеет баг с кодировкой путей.

**Глобальное правило:** Всегда использовать этот хак для файлов в директориях с русскими символами в имени.
