# Balloo Messenger — Grafana Dashboard Provisioning

apiVersion: 1

providers:
  - name: 'Balloo Dashboards'
    orgId: 1
    folder: 'Balloo'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
