# Приёмка: прод (P27–P32 + этап 51), desktop, Android — чек-лист с полями

> Создан: 2026-09-20 (этап 51). Основной документ: `tickets/prod-deploy-handoff-20260912.md` (§1.0.5 — решения владельца, §6-бис — деплой, §7.3 — правки этапа 51).
> Формат ответа по каждому пункту: `✅ принято` или `❌ замечание №__` + короткое описание (что видно, где, скриншот по желанию).

---

## ⚠️ Блок 0 — предусловия (до приёмки блока 2)

Правки этапа 51 (реальный QR pair-token) **на проде ещё НЕ задеплоены** — деплой этапа 47 (`14243f6`) был до них. Блоки 1 (P27–P31) принимаются на текущем проде как есть. Для блока 2 сначала деплой (3 команды из §6-бис):

```bash
cd ~/balloo && git pull --ff-only origin main && git log --oneline -1   # ждём 5bdc349
```
```bash
cd ~/balloo/docker/prod && docker compose -f docker-compose.local.yml --env-file .env.production build web server
```
```bash
cd ~/balloo/docker/prod && docker compose -f docker-compose.local.yml --env-file .env.production up -d web server
```

- [ ] Деплой этапа 51 выполнен: `5bdc349` на сервере, `balloo-web Started`, `balloo-server Started`, 5/5 Up — вывод: ____________

---

## Блок 1 — прод web: P27–P31 + Rambler (деплой этапа 47, принимать сейчас)

Инкогнито-окно, `https://balloo.su/#/login`:

- [ ] **P27 — тема russian:** переключатель темы → «русская»: фон — градиент флага РФ (45°, белый→синий→красный), виден **сквозь** полупрозрачные топбар и карточку (не «вторая тёмная») — ✅/❌ №__
- [ ] **P28 — прокрутка формы входа:** форма прокручивается, верх карточки не уходит под шапку — ✅/❌ №__
- [ ] **P28 — прокрутка формы регистрации** (`/#/register`): то же + **подвал регистрации виден** — ✅/❌ №__
- [ ] **P29 — отступы:** форма не наезжает на шапку/подвал, отступы сверху и снизу — ✅/❌ №__
- [ ] **P31 — аватарка (регистрация):** загрузка картинки → предпросмотр показывает **картинку** (октагон), не стену base64-текста — ✅/❌ №__
- [ ] **P30 — QR-блок** (`/#/add-device`, кнопка «Войти с другим устройством»): QR-карточка, таймер 60 сек (краснеет на ≤10), «Обновить код» сбрасывает таймер — ✅/❌ №__
- [ ] **P32-репо — Rambler без 429:** клик по 4 квадратам OAuth подряд (Rambler дважды) — **сырого JSON 429 нет**; Rambler → возврат на `/#/login` с дружелюбным «Вход через Rambler пока не настроен» — ✅/❌ №__
- [ ] **P32-панели — Яндекс/VK/Mail.ru:** ведут на страницы провайдеров (ошибки провайдеров до §6-тер — ожидаемы, фиксируем текст ошибки для панелей): Яндекс: ______ / VK: ______ / Mail.ru: ______
- [ ] `docker inspect -f 'restarts={{.RestartCount}}' balloo-server` → 0 — вывод: ______

---

## Блок 2 — прод web: этап 51 (после деплоя из блока 0)

- [ ] **QR — реальный код:** `/#/add-device` показывает код `balloo://pair/<48 hex>` (не `…` и не заглушка), при истечении таймера код обновляется сам — ✅/❌ №__
- [ ] **QR — полный поток:** на другом браузере/устройстве войти (email) → подтвердить код (пока confirm-клиента в web нет — проверка статуса через curl, либо принимаем UI-часть и полный e2e в v2): решение владельца: принять UI-часть / требовать e2e сейчас — ______
- [ ] **503 не ожидаем** (Redis на проде есть): код выдаётся без ошибки «сервис недоступен» — ✅/❌ №__
- [ ] Общий smoke после деплоя: `https://balloo.su` 200, вход по email работает, `https://api.balloo.su/health` — пароль probe из `~/balloo-probe-credentials.txt` — ✅/❌ №__

---

## Блок 3 — desktop (инсталляторы `packages/desktop/release/` от 2026-09-20)

Артефакты: `balloo_1.0.0_amd64.deb`, `_x86_64.AppImage`, `_x64.tar.gz` (+ arm64). Правки этапа 51 для desktop аддитивны — пере-релиз не требуется до приёмки.

- [ ] **Установка/запуск** (deb или AppImage): приложение стартует, окно открывается — ✅/❌ №__
- [ ] **Экран входа:** форма по макету (сетка OAuth 2×2, язык, тема) — ✅/❌ №__
- [ ] **Вход по email** на прод-API — ✅/❌ №__
- [ ] **Тема russian:** градиент флага РФ на фоне — ✅/❌ №__
- [ ] **Основные экраны** (чаты/контакты/настройки — какие открыл, что не так): ______
- [ ] **Add-device (QR):** экран показывает QR — ✅/❌ №__
- [ ] Прочие замечания (номер экрана + что видно): ______

## Блок 4 — Android (как тестируем: APK / `expo start` — указать: ______)

Реализованные экраны: Login, Register, TwoFactor, ResetPassword, ChatList, ChatView, Profile, Settings, Contacts, CreateGroup, Blog, Knowledge, Hiring.

- [ ] **Запуск приложения** — ✅/❌ №__
- [ ] **Тема russian (новое, этап 51):** на входе/регистрации — градиент флага РФ (не сплошной тёмный) — ✅/❌ №__
- [ ] **Вход по email** — ✅/❌ №__
- [ ] **Список чатов / чат** (отправка сообщения) — ✅/❌ №__
- [ ] **Контакты / Профиль / Настройки** — ✅/❌ №__
- [ ] **Блог** — ✅/❌ №__
- [ ] Прочие замечания (экран + что видно): ______
- [ ] **Решение по 13 нереализованным экранам** (stories, media-viewer, donate, my-donates, archive, calls-history, chat-attachments, report-message, poll-editor, voice-record, empty-chat, my-devices, add-device): пилить до релиза / принимаем как есть → v2 — ______
- [ ] **Решение по OAuth в Android:** реальный flow с deep link `balloo://` сейчас / заглушка до v2 — ______

---

## Блок 5 — решения владельца (отвечено 2026-09-20)

1. **Панели OAuth:** настраиваем сразу под `api.balloo.su` — один раз, после тикета №0 (мультитикет поддоменов). — ✅ решено
2. **Порядок дальше:** сначала переводим и открываем ВСЕ домены (мультитикет №0–№6), потом приёмка (блоки 1–4 этого чек-листа). Альфа — после доменов по отдельной команде. — ✅ решено
3. **Ребут:** после всех работ, после которых возможны регистрация/авторизация (все провайдеры + почта/пароль), общение, рассылка приглашений, создание и использование приглашений. Критерий записан в §10 handoff-документа (этап 52). Внимание: email-рассылка приглашений (`/api/v1/invites`, docs/04) НЕ реализована — задача в план до ребута. — ✅ решено
4. **Боевой аккаунт:** сразу после ремонта авторизации/регистрации. — ✅ решено
5. **`validate-urls.mjs`:** предложение ассистента внесено в §1.0.5 №1 handoff-документа (сканер URL монорепо, частью тикета №0) — ждёт решения владельца. — ⏳

> **Следствие для блоков 1–4:** приёмка перенесена на момент после открытия всех доменов; их пункты будут проверяться уже под новой схемой (для прод-блока — с учётом тикета №0: API на `api.balloo.su`). Блок 0 (деплой) выполняется в составе работ по доменам.

---

## Команда для новой сессии

```
прочитай tickets/prod-deploy-handoff-20260912.md и tickets/Приёмка_20260920_P27-P51_desktop_android.md,
выполни деплой этапа 51 (блок 0 чек-листа, 3 команды), затем проведи приёмку по чек-листу
```

В новой сессии: сначала деплой (если ещё не сделан), затем владелец заполняет блоки 1–4 и отвечает на блок 5 — я вношу результаты в handoff-документ (новый этап журнала, статусы P27–P31/P30/P32, решения в §1.0.5).
