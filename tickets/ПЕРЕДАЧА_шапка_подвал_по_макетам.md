# Передача сессии: шапка/подвал по макетам (P38)

> Создан 2026-09-24 в конце переполненной сессии. Новый контекст — отсюда,
> без пересказа. Команда запуска: «прочитай tickets/ПЕРЕДАЧА_шапка_подвал_по_макетам.md
> и продолжай».

## 0. Обязательные правила (уже в репозитории — прочитать)

- `AGENTS.md` — раздел «🚫 Запрет на выдумки в отчётах» (2026-09-23). Любое
  «сделано/проверено» — только с выводом команды/скриншотом/JSON. В этой
  сессии я снова выдумывал (WhatsApp-модуль, «миграции», несуществующие пути
  `frontend/`, `packages/backend/`, `pages/`) — не повторять. **WhatsApp-задач
  нет, модуля в репо нет — не выдумывать.**
- Порядок: макет (`mockups/`) → код. Эталон шапки/подвала:
  `mockups/assets/common.css` + `mockups/assets/common.js` (см. §2).
- `tickets/Мультитикет_поддомены_C.md` §6.1 — после каждой задачи: пуш +
  блок команд деплоя на сервер + чек-лист мест проверки. **Путь прод-каталога
  ПОДТВЕРЖДЁН выводом сервера 2026-09-24: `/home/cfr_balloo/balloo`**
  (пользователь `cfr_balloo`, хост `aedgar`). Ранее записанный
  `/home/aedgar192/balloo` — ошибка, исправлена в мультитикете (§2 и §6.1).
  Факты с прод-сервера (ветка, compose-файл, порт web, nginx) — в
  `tickets/ТЗ_деплой_P37-1_на_прод.md` §0.

## 1. Текущая задача (постановка пользователя, дословный смысл)

Поправить страницы: **шапка и подвал, соответствующие макетам**.
- В шапке — **отдельное левое и правое меню**.
- **Правое меню — сворачиваемое**, в нём:
  - смена языка;
  - смена темы;
  - юридические страницы;
  - для авторизованных: страницы авторизованных, **выйти**, **мультиаккаунт**;
  - для неавторизованных: **войти / зарегистрироваться**.

## 2. Эталон из макетов (проверено чтением файлов 2026-09-24)

- **Левое меню**: `.topbar__logo` + `.topbar__dropdown` (hover/клик) — список
  узлов `BALLOO_NODES` (common.js:376–385): Balloo/Admin/Command/Features/
  History/Blog/Download/API Docs. CSS: common.css:188–252, 2034–2052
  (`--node`/`--node--current`).
- **Правое меню (этот же контракт!)**: common.js `buildRightMenu()`
  (**строки 433–554**) — «Unified across all nodes — built by common.js».
  - Триггер `.topbar__menu-btn` (common.css:1820–1840): гость → маскот 🐻,
    авторизован → октагон-аватар.
  - Панель `.right-menu` (common.css:1842–1966): `position:fixed; top:56px;
    right:0; width:280px`, классы `right-menu--open`, `__section-label`,
    `__item(__icon/__label/__item--active)`, `__divider`,
    `__submenu(--open)`, `__subitem(--active/__check)`, `__auth-guest`.
  - Состав (common.js:465–520): «Язык / Language» → подменю; «Тема» → подменю;
    разделитель; **Техподдержка / Приватность / Правила пользования** (юрид.);
    разделитель; «Аккаунт»:
    - гость: кнопки **Вход** + **Регистрация** (`.right-menu__auth-guest`);
    - авторизован: **Профиль / Настройки / Выйти**.
- Темы: `BALLOO_THEMES` = dark 🌙 / light ☀️ / russian 🇷🇺 «Наша».
- Языки в мокапе: `BALLOO_LANGS` = 6 (ru,en,zh,fr,be,hi). **Контракт web —
  20 языков** (AGENTS.md, `mockups/data_schema.json → conventions.languages`)
  — в подменю показывать контрактные 20 (группы: русские/дружественные/остальные).
- Подвал `.balloo-footer` (common.css:1973–2027): © слева, ссылки справа —
  уже реализован в `@balloo/ui` (Footer.tsx), сверять при случае.
- **Решения по расхождениям (записать в тикет, не молчать):**
  - «Мультиаккаунт» в common.js НЕ описан — это новое требование пользователя.
    Добавить пунктом в «Аккаунт» авторизованной секции; ссылка — на страницу
    аккаунтов (в web есть `/accounts`? проверить в `router/index.tsx`; макет
    `mockups/balloo-su/accounts.html` существует).
  - Cookies в правом меню мокапа нет (есть только в подвале) — в меню не добавлять.

## 3. Что в коде сейчас (проверено)

- `packages/ui/src/components/Topbar.tsx` — рендерит `.topbar__right` с
  `LanguageSwitcher` (свой дропдаун) + `ThemeSwitcher` (3 кнопки) **inline**.
  Это НЕ по макету: надо заменить на сворачиваемое `RightMenu`.
- `packages/ui/src/components/TopbarMenu.tsx` — левое меню уже есть
  (DEFAULT_TOPBAR_ITEMS: чат/admin/command/features/history/download/doc).
  Сверить с BALLOO_NODES (нет Blog) — поправить состав.
- `packages/ui/src/styles/chrome.css` (301 строка) — есть `.topbar__menu-btn`
  (159–176), НО блоков `.right-menu*` нет → перенести из common.css:1820–1966.
- `packages/web/src/components/chrome/`: `AppTopbar.tsx` (обёртка Topbar +
  useNavigate), `AppFooter.tsx` (SPA-перехват ссылок), `PageChrome.tsx`
  (topbar→content→footer). MainLayout/AdminLayout/CommandLayout/blog-экраны
  используют AppTopbar — правки в @balloo/ui подхватятся всеми.
- Авторизация web: `packages/web/src/store/authStore.ts` + AuthProvider
  (проверить имена), GET /api/auth/... — curl в прошлой сессии: у старого
  docker-образа `/api/auth/me` нет, login отдаёт токены в теле БЕЗ Set-Cookie.
  Для «авторизован/гость» в RightMenu нужен проп из web (`auth` + `user` +
  `onLogout`), @balloo/ui не должен сам ходить в API.

## 4. Состояние P37 (предыдущая задача, чек-лист)

- ✅ **Задача 1** (подвал на 6 blog-экранах): коммит `97d7077` **запушен**
  (3115a0a..97d7077). Живой Chromium: 6/6 `подвал=1440×36` — см.
  `.check/p37-chrome-audit.json`, скрипт `scripts/p37-chrome-audit.cjs`.
- ⚠️ **Задачи 2–5 НЕ закоммичены** (uncommitted в рабочем дереве):
  - `packages/web/src/screens/shared/NotFoundScreen.tsx` — добавлен AppFooter (404);
  - `packages/web/src/screens/admin/InstallScreen.tsx` — обёрнут PageChrome;
  - `packages/web/src/screens/specifity/SpecifityScreen.tsx` — самодельный
    топбар → PageChrome (title="Спецификация", инфо-строка в right);
  - `packages/web/src/screens/command/CommandLayout.tsx` — шапка вынесена
    НАД сайдбар на всю ширину (было 1200×56 → стало 1440×56);
  - `packages/web/src/screens/landing/ForKassaScreen.tsx` — AppFooter вынесен
    из контейнера 800px (было 752×36 → 1440×36).
  После правок прогонялся: tsc 0, vitest 158/158, build OK, аудит живым
  браузером: /install /spec /command /for_kassa /zzz-not-exist — все OK
  (шапка 1440×56, подвал 1440×36). **Но это было ДО начала работ над
  шапкой — перепроверить после правок @balloo/ui.**
- **Задача 6** (аудит 36 маршрутов, требующих сессию) — не сделана. Локально
  подняты docker-контейнеры `balloo-server :3100` (старый образ!),
  `balloo-postgres` (в прошлой сессии применял миграции из
  `packages/shared/prisma/migrations/` к БД `balloo` — 50 таблиц, и создал
  копию `balloo_verify`; тестовый пользователь `p38.audit@example.com` /
  `P38audit!2026`). Для живого входа нужен dev-сервер из текущего кода
  (`pnpm dev:server`), он ставит httpOnly-куки (authController setAuthCookies).
- Известный баг (найдён, не исправлен): `packages/web/src/services/api.ts:395`
  `deleteBlogPost` — `/api/blog/posts/${postId}` в **одинарных** кавычках.

## 5. План P38 (делать по шагам, каждый шаг — коммит+пуш+деплой-блок)

1. Перенести `.right-menu*` + `.topbar__menu-btn` CSS из common.css в
   `packages/ui/src/styles/chrome.css` (дословно, блоки 1815–1966).
2. Новый `packages/ui/src/components/RightMenu.tsx`:
   - пропсы: `auth: 'guest'|'user'`, `userLabel?`, `onNavigate(to)`,
     `accountItems?: {key,to,icon}[]` (web передаёт Профиль/Настройки/
     Мультиаккаунт), `onLogout?()`, `guestActions?: {loginTo, registerTo}`;
   - язык/тема — sub-меню из `useUIStore` (SUPPORTED_LANGUAGES — 20,
     THEMES — 3 с лейблами «🌙 Тёмная/☀️ Светлая/🇷🇺 Наша»);
   - юр. пункты: Техподдержка `/support`, Приватность `/privacy`,
     Правила `/rules` (в web есть legal-маршруты; /support — проверить);
   - поведение как в common.js: toggle по кнопке, подменю ▾/▴, закрытие по
     клику вне; aria-атрибуты; Esc — закрыть.
3. `Topbar.tsx`: в `.topbar__right` оставить `right` (контекст сайта) +
   `<RightMenu {...}/>`. `showSwitchers` убрать/переиспользовать.
   LanguageSwitcher/ThemeSwitcher оставить экспортируемыми (используются
   внутри RightMenu или в других местах — проверить).
4. `AppTopbar.tsx`: прокинуть auth-контент из web (authStore): пользователь →
   авторизованный режим, аватар-триггер с инициалами; выйти → logout-API +
   navigate('/login'); мультиаккаунт → маршрут (см. accounts.html макет).
5. Проверить все места, где экраны передают `right` (MainLayout/CommandLayout/
   blog): аватар/колокольчик не должны дублировать триггер меню.
6. i18n: ключи пунктов меню на все языки контракта, где есть переводы
   (`packages/shared` translations) — как минимум ru/en, остальное fallback.
7. Прогнать: `pnpm --filter @balloo/web exec tsc --noEmit`, vitest, build;
   аудит `node scripts/p37-chrome-audit.cjs` (поднять mock :3199 + vite :3211
   — команды в §6) → скриншоты `.check/p38-*`; при возможности — живой вход
   через dev-сервер и проверка авторизованной версии меню.
8. Коммит + пуш + блок деплоя и чек-лист (по §6.1 мультитикета; путь прод-
   каталога уточнить у пользователя).

## 6. Как запускать локальные проверки (проверенные команды)

```bash
cd "/home/ivan/Рабочий стол/проекты/balloo"
node scripts/p34-mock-api.cjs &                      # :3199
(cd packages/web && npx --yes vite --port 3211 --strictPort) &   # :3211
node scripts/p37-chrome-audit.cjs                    # 72 маршрута, Chromium
# после прогона:
jq -r '.[] | select(.problems|length>0) | .route+" :: "+(.problems|join(";"))' .check/p37-chrome-audit.json
```

tsc/vitest/build — в `packages/web` (npx --yes ... , долго: до 10 мин).

## 7. Хвосты/контекст

- `git log`: HEAD был `97d7077` (задача 1 P37) + uncommitted файлы §4.
- Не коммиченное из прошлых сессий: `scripts/audit-i18n.mjs`,
  `scripts/p36-russian-sweep.cjs` (untracked) — не трогать без нужды.
- Правый край: в `TopbarMenu` левое меню открывается hover+клик — OK по макету.
- Все пути в репо: `packages/web/src/...` (никаких `frontend/`, `src/pages/`).
