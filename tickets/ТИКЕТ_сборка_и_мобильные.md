# Мультитикет: Сборка + Android

**Проект:** Balloo Messenger  **Дата:** 2026-08-09  **Статус:** todo

> **ВАЖНО:** Финализация: починить сборку server+web + написать Android. iOS — НЕ ДЕЛАЕМ (блокировка AppStore).
> Server и Web — код ЕСТЬ (735 файлов).

## 📋 Как вызывать

```
прочитай документ: tickets/ТИКЕТ_сборка_и_мобильные.md и выполни тикет № X
```

---

## 📊 ФАКТИЧЕСКОЕ состояние

### ✅ ЧТО ЕСТЬ (не писать)

| Компонент | Статус | Файлов |
|---|---|---|
| Shared | ✅ Готов | 47 |
| Server | ✅ Готов | 243 |
| Web | ✅ Почти | 287 |
| Desktop | ✅ Готов | 115 |

### ❌ ЧЕГО НЕТ

| Компонент |
|---|
| Mobile Android — папки есть, кода 0 |
| Mobile iOS — НЕ ДЕЛАЕМ |

### ⚠️ СЛОМАНО

1. `packages/web/src/router/index.tsx:54` — `InstallScreen` нет
2. `packages/web/src/screens/auth/RegisterScreen.tsx:200` — `provider="max"` невалидный
3. `node_modules` не установлен

---

### ТИКЕТ №1 — Починить сборку web+server

**Зависит от:** []  **Оценка:** ~15 мин

#### Сделать

1. `packages/web/src/router/index.tsx:54` — `InstallScreen` импортируется, файла нет
   - Посмотреть `installController.ts` на сервере
   - Создать экран или удалить импорт

2. `packages/web/src/screens/auth/RegisterScreen.tsx:200` — `provider="max"` невалидный
   - Тип: `"yandex" | "vk" | "mailru"` — удалить Max или заменить на Yandex

3. `pnpm build` — убедиться, что проходит

#### Проверка
```bash
pnpm build
```

#### Handoff
Переходить к тикету №2.

---

### ТИКЕТ №2 — Запустить server+web в dev-режиме

**Зависит от:** [1]  **Оценка:** ~30 мин

#### Сделать

1. `pnpm install`
2. `cp .env.example .env`
3. Запустить PostgreSQL
4. `npx prisma migrate deploy` + `npx prisma db seed` в `packages/shared/`
5. `pnpm dev:server` (порт 3100)
6. `pnpm dev:web` (порт 5173)
7. Проверить: `curl http://localhost:3100/health` → OK

#### Проверка
```bash
pnpm install && cp .env.example .env
cd packages/shared && npx prisma migrate deploy && npx prisma db seed
cd ../../ && pnpm dev:server & pnpm dev:web &
curl http://localhost:3100/health
```

#### Handoff
Переходить к тикету №3 — Android.

---

### ТИКЕТ №3 — Android: настройка Expo + shared

**Зависит от:** [2]  **Оценка:** ~30 мин

#### Сделать

1. Проверить `packages/mobile-android/package.json`
2. Создать `src/App.tsx` — точка входа
3. Создать `src/router/index.tsx` — React Navigation
4. Подключить `@balloo/shared` через workspace
5. Базовый store (Zustand) + компоненты (Button, Avatar, Input)

#### Проверка
```bash
cd packages/mobile-android && npx expo start
```

#### Handoff
Переходить к тикету №4 — auth экраны.

---

### ТИКЕТ №4 — Android: экраны аутентификации

**Зависит от:** [3]  **Оценка:** ~2 часа

#### Сделать

Сверять с `mockups/mobile/` и `mockups/balloo-su/`:

1. `LoginScreen.tsx` — email + пароль, OAuth (Yandex, VK, Mail.ru)
2. `RegisterScreen.tsx` — email, пароль, OAuth, валидация
3. `TwoFactorScreen.tsx` — ввод 6-значного кода
4. `ResetPasswordScreen.tsx` — email → код → новый пароль

Подключить: API сервис, Auth store (Zustand), OAuth кнопки

#### Проверка
```bash
cd packages/mobile-android && npx expo start
```

#### Handoff
Переходить к тикету №5 — layout + чаты.

---

### ТИКЕТ №5 — Android: layout + чаты

**Зависит от:** [4]  **Оценка:** ~2 часа

#### Сделать

1. `MainLayout.tsx` — bottom nav (чаты, контакты, поиск, настройки)
2. `ChatListScreen.tsx` — список чатов (сверить с `mockups/balloo-su/chats.html`)
3. `ChatViewScreen.tsx` — просмотр чата (сверить с `mockups/balloo-su/chat.html`)
4. WebSocket для real-time

#### Проверка
```bash
cd packages/mobile-android && npx expo start
```

#### Handoff
Переходить к тикету №6 — профили, настройки, блог.

---

### ТИКЕТ №6 — Android: профили, настройки, каналы, блог

**Зависит от:** [5]  **Оценка:** ~3 часа

#### Сделать

1. `ProfileScreen.tsx` — профиль (сверить с `mockups/balloo-su/profile.html`)
2. `SettingsScreen.tsx` — настройки
3. `ChannelViewScreen.tsx` — канал
4. `BlogScreen.tsx` — блог
5. `SearchScreen.tsx` — глобальный поиск
6. `StoriesScreen.tsx` — истории
7. `ContactsScreen.tsx` — контакты

#### Проверка
```bash
cd packages/mobile-android && npx expo start
```

#### Handoff
Переходить к тикету №7 — финальная проверка.

---

### ТИКЕТ №7 — Финальная проверка

**Зависит от:** [6]  **Оценка:** ~1 час

#### Сделать

1. `pnpm build` — всё собирается
2. `npx expo export` в `packages/mobile-android/` — Android бандл
3. `docker compose up -d` — PostgreSQL + Server + Web
4. Проверить: `curl http://localhost:3100/health` + `curl http://localhost:8080/health`
5. Протестировать web-клиент: регистрация, логин, отправка сообщения

#### Проверка
```bash
pnpm build
docker compose up -d
curl http://localhost:3100/health
```

---

## 📊 Оценка

| Тикет | Что | Оценка |
|---|---|---|
| №1 | Починить сборку (2 ошибки) | ~15 мин |
| №2 | Запустить server+web | ~30 мин |
| №3 | Android: настройка Expo | ~30 мин |
| №4 | Android: auth экраны | ~2 ч |
| №5 | Android: layout + чаты | ~2 ч |
| №6 | Android: профили, настройки, блог | ~3 ч |
| №7 | Финальная проверка | ~1 ч |
| **ИТОГО** | **7 тикетов** | **~9 часов** |

> iOS — НЕ ДЕЛАЕМ (блокировка AppStore).

---

## 📝 Лог выполнения

### Тикет №1
- Статус: ✅ done | Выполнен: 2026-08-09 | Файлы: InstallScreen.tsx, RegisterScreen.tsx, paymentService.ts | Результат: Сборка web+server+desktop прошла успешно

### Тикет №2
- Статус: todo | Выполнен: — | Файлы: — | Результат: —

### Тикет №3
- Статус: todo | Выполнен: — | Файлы: — | Результат: —

### Тикет №4
- Статус: todo | Выполнен: — | Файлы: — | Результат: —

### Тикет №5
- Статус: todo | Выполнен: — | Файлы: — | Результат: —

### Тикет №6
- Статус: todo | Выполнен: — | Файлы: — | Результат: —

### Тикет №7
- Статус: todo | Выполнен: — | Файлы: — | Результат: —
